To run go files you'll need to install go
To run the file directly on your terminal use "go run *name of the file*"
To create a build file run "go build -o *builfilename *gofilename"
For window users run "go build -o *buildfilename *gofilename
